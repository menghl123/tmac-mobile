import { TmacSideMenuToggleDirective } from './tmac-side-menu-toggle.directive';

describe('TmacSideMenuToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new TmacSideMenuToggleDirective();
    expect(directive).toBeTruthy();
  });
});
