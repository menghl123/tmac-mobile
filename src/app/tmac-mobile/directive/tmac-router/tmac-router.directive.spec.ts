import { TmacRouterDirective } from './tmac-router.directive';

describe('TmacRouterDirective', () => {
  it('should create an instance', () => {
    const directive = new TmacRouterDirective();
    expect(directive).toBeTruthy();
  });
});
